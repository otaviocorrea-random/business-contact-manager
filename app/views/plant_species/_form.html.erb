<%= form_with(model: plant_specy) do |form| %>
  <% if plant_specy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(plant_specy.errors.count, "error") %> prohibited this plant_specy from being saved:</h2>

      <ul>
        <% plant_specy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: business_contact) do |form| %>
  <% if business_contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(business_contact.errors.count, "error") %> prohibited this business_contact from being saved:</h2>

      <ul>
        <% business_contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :type, style: "display: block" %>
    <%= form.number_field :type %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
